import 'package:flutter/material.dart';

class DayWeek extends StatelessWidget {
  const DayWeek({super.key});

  @override
  Widget build(BuildContext context) {
    return const MaterialApp(
      home: Scaffold(
        backgroundColor: Color.fromARGB(255, 255, 151, 151),
      ),
    );
  }
}
